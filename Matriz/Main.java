/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package javaapplication1;

/**
 *
 * @author alunoinf
 */
public class Main {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
/*
        int m, n, p, i, j, k, m1[][], m2[][], m3[][];
        m = IO.readInt();
        n = IO.readInt();
        p = IO.readInt();
        m1 = new int[m][n];
        m2 = new int[n][p];
        m3 = new int[m][p];

        for (i = 0; i < m; i++)
              for (j = 0; j < n; j++) m1[i][j] = IO.readInt();
        for (i = 0; i < n; i++)
              for (j = 0; j < p; j++) m2[i][j] = IO.readInt();

        IO.writeln();

        for (i = 0; i < m; i++) {
        for (j = 0; j < p; j++) {
                 m3[i][j] = 0;
            for (k = 0; k < n; k++)
                m3[i][j] = m3[i][j] + m1[i][k] * m2[k][j];

              IO.write(m3[i][j], 3);
        }
              IO.writeln();
        }
 */
 
 System.out.println(Matriz);
    }

}
